
## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5432/stratego
spring.datasource.username= postgres
spring.datasource.password= stratego

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

spring.data.rest.base-path=/api

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

spring.security.oauth2.client.registration.google.client-id=<id goes here>
spring.security.oauth2.client.registration.google.client-secret=<secret goes here>

spring.security.oauth2.client.registration.facebook.client-id=<id goes here>
spring.security.oauth2.client.registration.facebook.client-secret=6<secret goes here>
spring.security.oauth2.client.provider.facebook.user-info-uri=https://graph.facebook.com/me?fields=id,name,email,picture

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=<username>
spring.mail.password=<password>
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true